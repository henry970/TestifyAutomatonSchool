
name: Merege CI
on: [ pull_request ]
jobs:
  Test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ Macos-latest, Ubuntu-latest, Windows-latest ]
    steps:
      - uses: actions/checkout@v2
      - run: python main.py
